# EventBus / NATS benchmark artifacts

JSON outputs from `scripts/bench_eventbus_publish.py`.

- `eventbus_publish_latest.json` — most recent run (committed as a reference).
- `eventbus_publish_YYYYMMDD_HHMMSS.json` — timestamped runs (gitignored; local only).

See [docs/EVENTBUS-LOAD-TEST.md](../../docs/EVENTBUS-LOAD-TEST.md) for how to
reproduce and how to interpret ceilings / bottlenecks.
